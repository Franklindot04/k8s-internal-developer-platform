apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "base-chart.fullname" . }}-test-connection"
  labels:
    {{- include "base-chart.selectorLabels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: curl
      image: curlimages/curl:latest
      command: ["echo", "Running Helm chart test (placeholder)..."]
  restartPolicy: Never
